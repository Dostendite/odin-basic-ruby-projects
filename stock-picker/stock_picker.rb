stock_prices = [17,3,6,9,15,8,6,1,10]

def stock_picker(prices_array)
  
  # Buy day & Sell day
  best_deal = [0, 0]

  # Loop through the array

  # Loop again to compare the
  # current number with the others

  # If the current number minus the
  # number compared against it
  # return higher than best_deal,
  # best deal[0] = current_number
  # best_deal[1] = compare_number

  # Then return best_deal

days = stock_picker(stock_prices)

puts days